# FastPost Social v3 - Python Dependencies
# Install with: pip install -r requirements.txt
# Then run: playwright install chromium

# Web Framework
flask==3.0.3
flask-cors==4.0.1

# Environment variables
python-dotenv==1.0.1

# Browser Automation (Stealth Posting)
# Newer Playwright pulls greenlet wheels for current Python (3.14+); avoid old pins that force source builds.
playwright>=1.49.0
playwright-stealth>=2.0.0

# Web Crawling / Scraping (BeautifulSoup uses built-in html.parser — lxml not required)
requests==2.32.3
beautifulsoup4==4.12.3

# Task Scheduling
APScheduler==3.10.4
pytz==2024.1

# Database (SQLite is built-in, but these help with migrations)
# Uncomment below if upgrading to PostgreSQL:
# psycopg2-binary==2.9.9
# SQLAlchemy==2.0.30

# Utilities
certifi==2024.6.2
