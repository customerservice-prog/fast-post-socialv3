# Render Blueprint — https://render.com/docs/blueprint-spec
# Persistent data: DB + Playwright browser profiles live under /data (requires a plan that supports disks on web services).
services:
  - type: web
    name: fast-post-socialv3
    runtime: python
    buildCommand: sh build.sh
    startCommand: sh start.sh
    disk:
      name: fastpost-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /app/.playwright-browsers
      - key: FLASK_DEBUG
        value: "0"
      - key: DATABASE_PATH
        value: /data/fastpost.db
      - key: PROFILES_DIR
        value: /data/browser_profiles
      - key: SECRET_KEY
        generateValue: true
